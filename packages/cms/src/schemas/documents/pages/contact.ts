import { PageDocument } from '../../../types';

export const schema: PageDocument = {
    name: 'contactPage',
    title: 'Contact',
    icon: '',
    // disabledActions: ['create', 'delete'],
    fields: [],
};
